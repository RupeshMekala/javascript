"use strict"; // treats all the js code as a newer version
// "use strict" is implemented by default

// alert("Hello") // this doesn't run because alert function is a part of broswer's javascript API
// It is not a part of core JS lang, so Node being an interpreter and run-time environment of Js 
// cannot run this

let name = "Rupesh";
let age = "20";
let isLoggenIn = true;

// integer(int) --> -(2^53 - 1) to (2^53 - 1)
// bigint 
// string
// boolean --> true/false
// null --> standalone value
// undefined --> declared but not assigned
// symbol --> used for uniqueness(will understand it while learning react.js)


//object

console.log(typeof undefined) // what will it return?
console.log(typeof null) // what will it return and why?(notes)

 